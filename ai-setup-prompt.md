# 项目 AI 协作体系搭建提示词（备份模板）

> 用途：新项目初始化时，将以下整段内容一次性发给 AI，即可完成本项目多轮对话才搭建好的 AI 协作体系。
> 使用前替换占位内容：`<项目名称>`、`<项目定位说明>`（如有）。

---

## 提示词正文（可整段复制）

请为当前项目一次性完成以下 AI 协作体系初始化，全程自动执行，无需逐项确认：

**1. 创建 `agent.md`（项目定位声明）**

写入：本项目目前只规划方案、写需求文档，不开工构建代码。工作范围：需求梳理与分析、方案设计、需求文档编写。约束：不进行代码开发、不搭建工程结构，不在当前阶段创建或修改业务代码文件。

**2. 初始化 Git**

执行 `git init`（默认分支 main）。

**3. 创建 `.gitignore`**

包含常见忽略项：系统文件（.DS_Store 等）、编辑器配置（.idea/、.vscode/）、依赖与构建产物（node_modules/、dist/、build/、日志文件）、Python（__pycache__/、.venv/）、临时文件；**不忽略 `.ai/` 文件夹**（AI 协作数据纳入版本库管理）。

**4. 创建 `.ai/` 目录体系（AI 协作数据区，纳入 Git 管理）**

```
.ai/
├── logs/        # 对话日志：按日期建文件 YYYY-MM-DD.log
├── plans/       # 规划方案文档
├── TODO.md      # 任务清单
└── context/     # AI 上下文目录
    ├── README.md       # 使用说明与读取顺序
    ├── project.md      # 项目概览：定位、阶段、约束
    ├── structure.md    # 目录结构与文件约定
    ├── decisions.md    # 关键决策记录
    └── progress.md     # 进展状态：已完成/进行中/下一步
```

各部分的运作机制与维护规则：

- **logs/**：每轮对话结束后，自动追加一条摘要到当天日志文件。格式为程序日志：`[YYYY-MM-DD HH:MM:SS] 级别 - 内容`。原则：**简洁，但要点记全**。日志只增不改，按日期归档。
- **plans/**：规划方案、需求文档。**AI 自动写入为主**：对话总结出方案即自动存入；用户明确要求保存时，按指令执行（辅助）。
- **TODO.md**：任务清单。任务状态变化时实时更新（进行中/规划中/已完成），并维护文件约定说明。
- **context/**：AI 上下文。按功能划分文件，**简洁但覆盖全内容**，目的是让 AI 只读此目录即可了解项目整体与细节，无需遍历日志。相关信息变化时更新：
  - project.md：项目定位、阶段、硬约束变化时更新
  - structure.md：目录结构或约定变化时更新
  - decisions.md：产生关键决策时追加（含日期、决策、理由）
  - progress.md：任务完成/状态变化时更新

**5. 更新 `agent.md`**

将上述 `.ai` 目录结构、各文件维护规则、运作要点（哪些内容由 AI 自动填充）写入 `agent.md`，作为后续 agent 的工作指引。

**6. 首次同步**

创建完成后，立即写入当天的对话日志和初始 context 文件（含本次初始化过程的决策记录），后续按规则持续自动维护。

---

## 可选补充

- 若项目有额外的硬约束（如：飞书文档仅写入指定文件夹），在第 1 步 agent.md 中一并声明。
- 若需要长期记住某条约定（如"日志简洁但要点记全"），可让 AI 存入长期偏好，跨项目生效。
