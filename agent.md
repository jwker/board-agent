# Agent 说明

本项目目前**只规划方案、写需求文档**，**不开工构建代码**。

## 当前工作范围

- 需求梳理与分析
- 方案设计
- 需求文档编写

## 约束

- 不进行代码开发、不搭建工程结构
- 不在本阶段创建或修改业务代码文件

---

# .ai 目录：AI 协作数据

`.ai/` 是 AI（agent）与用户协作产生的数据目录，纳入 Git 版本库管理。其中大部分内容由 agent **自动填充和维护**。

## 目录结构与运作机制

```
.ai/
├── logs/        # 对话日志
│   └── YYYY-MM-DD.log   # 按日期建文件
├── plans/       # 规划方案文档
├── TODO.md      # 任务清单
└── context/     # AI 上下文（供 AI 快速了解项目，不遍历日志）
    ├── README.md       # 使用说明与读取顺序
    ├── project.md      # 项目概览：定位、阶段、约束
    ├── structure.md    # 目录结构与文件约定
    ├── decisions.md    # 关键决策记录
    └── progress.md     # 进展状态：已完成/进行中/下一步
```

## Agent 自动维护规则

| 目录/文件 | 内容 | 触发时机 | 原则 |
|-----------|------|----------|------|
| `logs/YYYY-MM-DD.log` | 每轮对话摘要 | 每轮对话结束后 | 程序日志格式 `[时间] 级别 - 内容`；简洁但要点记全 |
| `plans/` | 规划方案、需求文档 | **AI 自动写入为主**：对话总结出方案即自动存入；用户要求保存时按指令执行（辅助） | 完整可读 |
| `TODO.md` | 任务清单及状态 | 任务变化时更新 | 状态实时同步 |
| `context/` | 项目概览/结构/决策/进展 | 相关信息变化时更新 | 简洁但覆盖全内容，供 AI 读取 |

## 运作要点

- 规划文档以 **AI 自动写入为主**：对话总结出方案即存入 `plans/`；用户明确要求保存时，按指令执行（辅助）
- context 是 AI 了解项目的主入口，日志仅作明细备查
- `.ai/` 纳入 Git 版本库管理，与代码一起维护
