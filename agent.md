# Agent 说明

本项目目前**只规划方案、写需求文档**，**不开工构建代码**。

## 当前工作范围

- 需求梳理与分析
- 方案设计
- 需求文档编写

## 约束

- 不进行代码开发、不搭建工程结构
- 不在当前阶段创建或修改业务代码文件

## Git 操作约定

- 用户说"提交"：仅执行 `git commit`，**不进行 push**
- 用户说"推送"：才执行 `git push`
- 未明确说明时，默认只提交不推送

---

# .ai 目录：AI 协作数据（纳入 Git 管理）

```
.ai/
├── logs/     # 对话日志：按日期建文件 YYYY-MM-DD.log
│             #   程序日志格式 [时间] 级别 - 内容；每轮对话后自动追加；简洁但要点记全
├── plans/    # 规划方案与需求文档（AI 自动写入为主，用户要求保存为辅）
└── TODO.md   # 任务清单，状态实时更新
```

## 维护规则

| 目录/文件 | 内容 | 触发时机 |
|-----------|------|----------|
| `logs/YYYY-MM-DD.log` | 每轮对话摘要 | 每轮对话结束后自动追加 |
| `plans/` | 核心逻辑方案、初步需求讨论、需求文档 | AI 自动写入为主，用户要求保存为辅 |
| `TODO.md` | 任务清单及状态 | 任务变化时更新 |

## 运作要点

- 需求与决策统一记录在 `plans/` 下文档（`initial-requirements.md`、`core-logic.md` 等）
- 日志为明细，需要细节时查阅；不设单独的上下文目录
